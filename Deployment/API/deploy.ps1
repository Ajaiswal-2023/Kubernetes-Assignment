# Apply ConfigMap
kubectl apply -f configmap.yaml

# Apply deployment
kubectl apply -f deployment.yaml

#Apply Service
kubectl apply -f service.yaml

# Apply horizontal pod scaling
kubectl apply -f hpa.yaml
